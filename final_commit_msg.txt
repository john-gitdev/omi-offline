fix: resolve all build errors and align architecture with PR 6200

Integrated and stabilized the Single-Owner BLE Connection Model:
- Native Android: Refactored OmiBleManager as a pure GATT wrapper with a serialized command queue. OmiBleForegroundService is now the connection owner.
- Native iOS: OmiBleManager unified to fire onDeviceReady after full service/characteristic discovery.
- Dart Architecture: Fully synchronized IDeviceService, DeviceConnection, and NativeBleTransport with the intent-based management model.
- Restored Local Features: Re-implemented omi-offline specific features (WAL sync, LED dimming, mic gain, rotate) that were lost during initial porting.
- Fixed Sync Logic: Rewrote SDCardWalSyncImpl to fix syntax errors and correctly use the refactored getBleStorageBytesStream API.
- Discovery Alignment: Fixed DeviceDiscoveryResult and DeviceLocator to support protocol-agnostic discovery.
- Build Success: Verified with a clean Flutter build (assembleDevDebug).
